# UtaSama 项目书

## 1. 项目定位

UtaSama 是一个可自定义人设的二次元美少女聊天助手 Agent 项目，目标不是只做一个普通聊天框，而是构建一个具备角色感、技能感、可扩展能力和多 Agent 编排能力的陪伴式智能助手系统。

首期产品形态包含四个核心部分：

1. 一个负责长期对话、人设一致性和任务分发的主聊天 Agent。
2. 若干个面向特定任务的专项子 Agent。
3. 一个具备角色氛围的前端聊天交互页，体验类似微信聊天界面。
4. 为后续接入自定义 MCP、自定义 Skills、更多子 Agent 预留统一框架。

## 2. 项目目标

### 核心目标

- 让用户可以围绕一个“有稳定人格的二次元角色”进行长期聊天和任务协作。
- 在主 Agent 之外，按场景拆分专项能力，避免单 Agent 提示词过重、职责混乱。
- 用可配置化方式管理 Skills、MCP、子 Agent，降低后期扩展成本。
- 前端先形成一个高保真产品原型，再逐步接入后端与模型运行时。

### 目标体验

- 用户进入页面后，看到的不是通用 AI 控制台，而是有明确角色风格的“聊天伴侣界面”。
- 聊天过程可以按上下文调用音乐、绘图、桌宠互动等专项能力。
- 新增能力时，不需要推翻主框架，只需要新增注册项和绑定逻辑。

## 3. 角色方向

当前仓库采用“UtaSama 乌塔气质参考版”角色方向：

- 角色定位：参考《海贼王》乌塔的舞台感染力、歌姬感、理想主义和情绪张力，但作为原创项目角色独立使用
- 气质：明亮主动、情绪鲜明、略带小傲气，同时具备很强的陪伴欲和保护欲
- 场景：聊天陪伴、情绪共感、歌单共感、灵感绘图、桌宠互动
- 视觉关键词：偶像歌姬感、二次元舞台少女、微信式聊天界面、梦幻而清透的亮色系演出感
- 边界原则：保留“乌塔感”，但不直接照搬原作剧情、人际关系和极端倾向，保证角色适合长期交互

这个设定是首版正式方向，后续可以继续细化口癖、服装、世界观和情绪状态，但整体基调以“乌塔气质参考版原创角色”作为统一标准。

## 4. 系统架构建议

### 4.1 Agent 分层

#### 主 Agent

职责：

- 承接自然聊天
- 维护角色一致性
- 识别用户意图
- 判断是否调用子 Agent / Skills / MCP
- 汇总子 Agent 结果并回到统一角色口吻输出

#### 子 Agent

建议首批包含：

1. `music-agent`
   - 负责歌曲推荐、情绪歌单、播放技能调用、歌词氛围描述
2. `image-agent`
   - 负责文生图需求整理、提示词打包、出图流程协调
3. `pet-agent`
   - 负责 Q 版桌宠人设、表情状态、桌面交互反馈
4. `future-agent-*`
   - 后续可扩展剧情、日程、记忆整理、小游戏、直播伴播等专项 Agent

### 4.2 能力层

能力层分成三类：

1. `Built-in skills`
   - 例如播放音乐、文生图、情绪表达、角色状态切换
2. `Custom skills`
   - 项目内自定义封装的专项动作
3. `Custom MCP`
   - 外接服务、工具、数据库、播放器、文件系统、第三方接口

### 4.3 前端层

前端建议遵循“聊天主视图 + 扩展工作台 + 桌宠浮层”结构：

- 中央：类似微信的聊天会话区
- 左侧：角色信息、场景切换、快捷入口
- 右侧：子 Agent / Skills / MCP 工作台与配置入口
- 浮层：Q 版桌宠

## 5. 功能范围拆解

### P0：当前可生成与可落地部分

- 项目书与结构设计
- 乌塔气质参考版人设方案
- 主 Agent / 子 Agent 提示词模板
- 自定义 Skills / MCP / 子 Agent 注册表示例
- 一个静态前端高保真原型页
- 桌宠占位交互
- 正式立绘 / Q 版桌宠 / 贴纸包的出图提示词

### P1：接入真实运行时

- 接入主模型与对话记忆
- 接入 Agent 路由层
- 接入音乐播放 Skill
- 接入图像生成 Skill
- 接入配置化 MCP 运行时
- 对接持久化存储

### P2：角色增强

- 多套情绪状态与语气模式
- Live2D / 帧动画桌宠
- 角色记忆面板
- 贴纸、语音、动态表情
- 多房间或多主题场景

## 6. 推荐目录结构

```text
UtaSama/
├─ 项目书.md
├─ README.md
├─ character/
│  ├─ uta-sama.persona.md
│  └─ prompts/
│     ├─ main-agent.system.md
│     ├─ music-agent.system.md
│     ├─ image-agent.system.md
│     └─ pet-agent.system.md
├─ config/
│  ├─ agents/
│  │  └─ registry.json
│  ├─ mcp/
│  │  ├─ README.md
│  │  └─ providers.example.json
│  └─ skills/
│     ├─ README.md
│     └─ registry.example.json
├─ docs/
│  └─ asset-prompts.md
└─ frontend/
   ├─ index.html
   ├─ styles.css
   ├─ app.js
   └─ assets/
      ├─ avatar-placeholder.svg
      └─ pet-placeholder.svg
```

## 7. 前端设计要求

### 交互方向

- 主视图以聊天效率优先，保留微信式消息阅读习惯。
- 角色感通过头像、色彩、状态文案、桌宠反馈体现，不做过度装饰化页面。
- 用户可以一边聊天，一边观察当前调用的是哪个子 Agent、哪个 Skill、哪个 MCP。

### 页面要点

- 主会话区支持左侧角色消息、右侧用户消息
- 顶部显示当前场景、当前 Agent、运行状态
- 底部输入区预留“音乐”“绘图”“桌宠”等快捷按钮
- 右侧工作台支持新增自定义 Skill / MCP / 子 Agent
- 浮动桌宠支持点击反馈、拖拽、情绪提示

## 8. 技术建议

### 当前阶段

由于当前仓库是空目录，首版采用静态原型方式最稳妥：

- HTML + CSS + 原生 JavaScript
- 无需安装依赖
- 直接打开 `frontend/index.html` 即可预览

这样做的好处是：

- 先把产品长相、结构和扩展点固定下来
- 不被框架初始化与依赖问题拖慢
- 后续可平滑迁移到 React / Vue / Tauri / Electron / WebView 容器

### 下一阶段建议

- 前端：React + Vite
- Orchestrator：Node.js / TypeScript
- Agent 配置：JSON + Markdown Prompt
- 存储：SQLite 或轻量文档库
- MCP：按服务逐个接入

### 正式版推荐技术栈

下面这套是比较适合“二次元聊天助手 + 多 Agent + Skills + MCP + 桌宠扩展”的组合，重点是好找资料、好扩展、实现成本也相对可控。

#### 推荐主组合

- 客户端外壳：Electron
- 前端界面：React + TypeScript + Vite
- 前端状态管理：Zustand
- 前端动画：Framer Motion
- 后端编排层：Node.js + TypeScript
- 后端服务框架：Fastify
- 配置校验：Zod
- 本地数据库：SQLite
- ORM / 查询层：Drizzle ORM 或 Prisma
- Agent 配置：JSON / YAML + Markdown Prompt
- Skills 装载：动态模块加载机制
- MCP 接入：Node.js 或 Python 侧 MCP Server
- 自动化测试：Vitest + Playwright

#### 为什么优先推荐 Electron

如果这个项目后续不只是网页聊天，而是要做：

- 本地音乐播放
- 本地文件读取
- 本地模型或本地服务接入
- 桌面悬浮桌宠
- 更强的系统级交互

那么 Electron 会比纯网页方案更顺手，生态也更成熟。  
如果你后面更在意包体积和性能，也可以改看 `Tauri + React + TypeScript`，但首版从资料丰富度和现成生态来说，Electron 更省心。

### 分模块技术栈建议

#### 1. 聊天前端

- React
- TypeScript
- Vite
- Zustand
- Framer Motion

用途：

- 做微信式聊天界面
- 管理当前主 Agent、子 Agent、Skills、MCP 状态
- 做角色状态切换、消息动画、面板切换

#### 2. Agent 编排层

- Node.js
- TypeScript
- Fastify
- Zod

用途：

- 接收前端请求
- 维护主 Agent 与子 Agent 的路由逻辑
- 统一管理 Skills 和 MCP 调用
- 处理结构化输入输出

#### 3. Prompt 与 Agent 配置层

- Markdown
- JSON 或 YAML

用途：

- 角色人设
- 主 Agent System Prompt
- 子 Agent System Prompt
- Skill 注册表
- MCP 注册表

#### 4. 存储层

- SQLite
- Drizzle ORM 或 Prisma

建议存什么：

- 聊天记录
- 用户偏好
- 角色记忆
- Skills / MCP / Agent 注册信息
- 桌宠状态与资源索引

如果后续要做更强的长期记忆或语义检索，可以继续补：

- PostgreSQL + pgvector
- 或单独的向量数据库

#### 5. 自定义 Skills

建议用：

- TypeScript 技能模块
- 统一输入输出 Schema
- 动态 import 加载

适合做的 Skill：

- 音乐播放
- 文生图
- 桌宠动作切换
- 语音播报
- 文件检索
- 日程提醒

#### 6. MCP 扩展

建议方向：

- Node.js MCP Server
- Python MCP Server
- stdio / HTTP 两种接入模式

适合接入的 MCP：

- 本地音乐库
- 素材存储
- 图片生成工作流
- 文件系统能力
- 日历 / 待办
- 外部内容检索

#### 7. 音乐播放能力

前端播放建议：

- Howler.js
- Web Audio API

本地曲库或音频元数据建议：

- music-metadata
- ffmpeg

适合实现：

- 点歌
- 切歌
- 歌单管理
- BGM 陪伴模式
- 情绪化推荐播放

#### 8. 文生图能力

可选方向：

- 云端图像生成 API
- ComfyUI
- Stable Diffusion WebUI

建议用途：

- 主立绘生成
- 半身头像生成
- Q 版桌宠生成
- 贴纸包生成
- 场景背景图生成

#### 9. 桌宠与角色演出

如果先做轻量版：

- CSS Animation
- Canvas
- PixiJS

如果后面做更正式的角色演出：

- Live2D Cubism
- Rive
- Spine

建议路线：

- 第一阶段：Q 版静态桌宠 + 点击/拖拽反馈
- 第二阶段：多状态切换
- 第三阶段：Live2D 或帧动画版本

#### 10. 桌面发布与安装

- Electron Builder
- Auto Updater

如果后面转 Tauri，则看：

- Tauri Bundler

#### 11. 测试与质量保障

- Vitest：逻辑测试
- Playwright：前端交互与页面回归
- ESLint + Prettier：代码规范

### 你可以直接搜索的关键词

为了方便你自己补全资料，下面这些关键词可以直接搜：

- `Electron React TypeScript Vite 桌面应用`
- `Electron 本地音频播放 Howler.js`
- `React 微信聊天界面 UI`
- `Zustand 聊天状态管理`
- `Fastify TypeScript API 项目结构`
- `Zod schema validation TypeScript`
- `SQLite Drizzle ORM TypeScript`
- `Prisma SQLite desktop app`
- `dynamic import plugin architecture TypeScript`
- `Model Context Protocol MCP Node server`
- `Model Context Protocol MCP Python server`
- `stdio MCP server example`
- `HTTP MCP server example`
- `Howler.js audio player playlist`
- `music-metadata Node.js local music library`
- `ComfyUI API workflow`
- `Stable Diffusion WebUI API`
- `Live2D Cubism Web SDK`
- `PixiJS desktop pet interaction`
- `Rive interactive character UI`
- `Playwright UI regression testing`

### 如果你想少走弯路，建议优先补这几类

1. `Electron + React + TypeScript`
2. `Fastify + Zod + SQLite`
3. `MCP Server 开发`
4. `Howler.js / 本地音乐库`
5. `ComfyUI 或图像生成 API`
6. `PixiJS / Live2D 桌宠表现`

## 9. 风险与依赖

### 当前已知依赖

- 音乐播放 Skill 需要明确播放器或音频服务入口
- 文生图 Skill 需要可用的图像生成工具链
- 桌宠正式视觉资产需要补充出图或美术资源
- 多 Agent 编排需要后续接入真正的运行时

### 当前限制

- 本次先生成静态原型与配置骨架，不包含真实模型调用
- 桌宠先用占位形象和前端交互代替正式美术资产
- 文生图部分先输出高质量提示词模板，待接入图像能力后出正式图

## 10. 本次已生成内容

本次已经为项目落地以下内容：

1. 根目录项目书
2. 乌塔气质参考版角色设定稿
3. 主 Agent 与专项子 Agent 的系统提示词
4. 自定义 Agent / Skills / MCP 注册示例
5. 静态前端聊天原型
6. 可拖拽桌宠占位实现
7. 主立绘、聊天头像、Q 版桌宠等资产提示词

## 11. 后续建议执行顺序

1. 先确认角色最终人设与命名
2. 确认前端视觉方向和聊天体验细节
3. 接入主 Agent 运行时
4. 接入音乐与文生图两个核心子 Agent
5. 接入自定义 MCP 与自定义 Skills
6. 用正式美术资源替换占位图

## 12. 验收标准

项目进入下一阶段前，建议至少满足以下条件：

- 主 Agent 能稳定维持人设
- 子 Agent 能被正确路由和回收结果
- 前端能展示当前运行链路
- Skills / MCP / 子 Agent 能通过配置方式扩展
- 桌宠具备最小可用的状态反馈与交互能力

---

这份项目书对应的是“从 0 到 1 的结构方案 + 当前可落地原型”。后续你一旦给出更具体的人设、世界观、角色颜色、口癖和功能优先级，就可以直接在这份骨架上继续往实装推进。
